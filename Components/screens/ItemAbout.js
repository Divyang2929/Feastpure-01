import React from "react";
import { View ,Text } from "react-native";
import Cards from "../homepages/Cards";
export default function ItemAbout ( {route, navigation} ){
return(
<Cards route={route} />

);

}